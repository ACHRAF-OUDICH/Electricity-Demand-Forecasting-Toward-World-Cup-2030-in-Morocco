version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"

# Indiquer explicitement à Sphinx où se trouve son fichier de configuration
sphinx:
  configuration: source/conf.py

python:
  install:
    - requirements: requirements.txt
